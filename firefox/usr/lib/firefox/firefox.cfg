//

// Disable Pocket
lockPref("extensions.pocket.enabled", false);

// Enable proper file picker
lockPref("widget.use-xdg-desktop-portal.mime-handler", 1);
lockPref("widget.use-xdg-desktop-portal.file-picker", 1);
lockPref("media.hardwaremediakeys.enabled", false);

// Disable alt key popping up menu bar
lockPref("ui.key.menuAccessKeyFocuses", false);

// Enable window title bar
lockPref("browser.tabs.inTitlebar", 0);

// Disable built-in password manager
lockPref("signon.rememberSignons", false);
